en el offset le indicas a partir de cual y en el limit la cantidad que queres por pagina
        ponele que tenes una tabla con 10 elementos y queres mostrar de a 5
        pones offset=0 y limit 5 si queres ver los primeros 5

Los controllers obtienen los parámetros del recurso a través de un arreglo asociativo 
que es enviado a los métodos con el nombre $params.


//throw new Exception("ingreso un valor no numerico")
manejar errores 
try {
    throw new Exception("Algún mensaje de error");
} catch(Exception $e) {
    echo $e->getMessage();
}

Filtro
linkTo el valor es el nombre de la columna 
equalTo (el enlace que voy a buscar quiero que sea igual a ..)




 $query= $this->db->prepare("SELECT * FROM bandas WHERE $linkTo = :$linkTo");
 PARA COLOCAR EL VALOR EN PDO EXISTE EL ENLACE DE PARAMETROS POR SEGURIDAD.
 colocas el mismo nombre de columna pero con dos puntos antes para decir que vamos a enlazar un parametro
 y el parametro lo enlazamos luego de preparar la sentencia sql.

$query->bindParam(":".$linkTo, $equalTo, PDO::PARAM_STR);
el bindParam enlaza parametros
Nombre columna , valor de lo que quiero enlazar y luego pdo de tipo string